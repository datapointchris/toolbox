version: 2

builds:
  - main: .
    binary: toolbox
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "toolbox_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

changelog:
  sort: asc
